{
  "name": "englewood-sites",
  "private": true,
  "type": "module",
  "workspaces": [
    "parasailing",
    "rentalboats"
  ],
  "scripts": {
    "dev:parasailing": "bun run --cwd parasailing dev",
    "dev:rentalboats": "bun run --cwd rentalboats dev",
    "build": "bun run --cwd parasailing build && bun run --cwd rentalboats build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.5.0"
  }
}
